input {
  beats {
    port => 5044
  }
}

filter {
  # Optional: parse Laravel log if needed
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "laravel-logs-%{+YYYY.MM.dd}"
  }
}
